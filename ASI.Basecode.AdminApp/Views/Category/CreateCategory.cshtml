@using ASI.Basecode.Services.ServiceModels
@model CategoryViewModel
@section styles {
    <link rel="stylesheet" href="~/css/createcategory.css" />
}

@{
    ViewData["Title"] = "Add Category";
    ViewBag.IsAddCategoryPage = true;
}


<div class="screen-container">
    <div class="inside-container">
        <form method="post" asp-action="CreateCategory">
            <div class="form-group">
                <label>CATEGORY NAME</label>
                <input type="text" class="form-control" asp-for="CategoryName">
                <span asp-validation-for="CategoryName" class="text-danger"></span><br>
            </div>
            <div class="form-group">
                <label>CATAGORY DESC</label>
                <textarea type="text" class="form-control" asp-for="CategoryDesc"></textarea>
                <span asp-validation-for="CategoryDesc" class="text-danger"></span><br>
            </div>
           
            <button type="submit" class="btn btn-primary">Submit</button>
            <a class="btn btn-secondary" asp-controller="Category" asp-action="TrainingCategories">
                BACK TO LIST
            </a>
        </form>
    </div>

</div>
@section scripts{
    <script>
        // Check for error message and display Toastr notification
        var errorMessage = '@TempData["ErrorMessage"]';
        if (errorMessage) {
            toastr.error(errorMessage);
        }
    </script>

}

